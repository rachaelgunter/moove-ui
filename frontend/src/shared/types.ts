// it might be worth using this technology in the future:
// https://github.com/dotansimha/graphql-code-generator#readme

// eslint-disable-next-line import/prefer-default-export
export enum Role {
  USER = 'USER',
  PAID_USER = 'PAID_USER',
  ADMIN = 'ADMIN',
}
